# План развития после пилота

> Обновление: пункт 3 (интеграция с AD) реализован — см.
> `printaudit/ad/`, `/admin/ad-users`, `/admin/ad-groups`. Пункт 1
> (централизованный сбор) реализован как MVP в ветке
> `feature/print-job-monitoring-multisite` — см.
> [docs/MULTISITE_ARCHITECTURE.md](MULTISITE_ARCHITECTURE.md) и пункт 1
> ниже про то, что именно сделано и что осталось. Ниже оставлены
> актуальные пункты и добавлены новые, вытекающие из этой ветки.
>
> Ещё одно обновление: в ветке `feature/printer-health-endpoint-forecasting`
> реализовано техническое состояние физических принтеров (не только
> очередей), endpoint-агент для USB/прямой печати и MVP-прогнозирование
> нагрузки/расходников/простоя — см.
> [docs/PRINTER_MONITORING_FORECASTING.md](PRINTER_MONITORING_FORECASTING.md).
> Пункт 2 ниже (точное определение цвета через SNMP-счётчики МФУ) частично
> сделан для ТРЕНДА нагрузки (счётчики устройства собираются как отдельная
> телеметрия, `PrinterCounterSample`), но НЕ используется для определения
> цвета отдельного задания печати — источник цвета для тарификации
> по-прежнему очередь/имя, как описано в этом пункте.

1. ~~**Централизованный сбор с 4 объектов.**~~ Сделано как MVP: агенты
   (`APP_MODE=agent`) отправляют события через durable outbox на
   центральный сервер (`APP_MODE=central`) по HTTPS с bearer-токеном на
   Print Server, идемпотентно по `(print_server_id, record_id)` — см.
   `webapp/agent_api.py`, `collector/agent_sync.py`,
   `docs/MULTISITE_ARCHITECTURE.md`. Что осталось на будущее: mTLS вместо
   (или в дополнение к) bearer-токену, доставка конфигурации агентам с
   центра (обновлённый `field_map` и т.п.), полноценный remote print-server
   manager (см. §9 и §10 того документа — сознательно не входило в объём
   этой ветки).

2. **Более точное определение цвета.**
   `is_color` теперь честно tri-state (True/False/None вместо тихого
   False при неизвестности — см. `printaudit/printers/resolver.py`,
   `docs/MULTISITE_ARCHITECTURE.md`, §8), но САМ способ определения цвета
   не изменился: по имени/настройке очереди печати (см.
   `docs/ADMIN_GUIDE.md`, раздел про разделение очередей). Более точные
   варианты на будущее: разбор EMF-спула задания (сложно и специфично для
   драйвера), либо WMI/SNMP-счётчики самого МФУ по цветным/ч.б. страницам
   (зависит от модели, снова упирается в вендора — то, что пилот сознательно
   обходит).

3. ~~**Интеграция с AD вместо CSV.**~~ Сделано — см. выше.

4. **Квоты и блокировки.**
   Windows Print Server не умеет нативно блокировать печать по лимиту —
   потребуется отдельный процесс, который periodически проверяет
   накопленный `cost`/`total_pages` пользователя и, при превышении, снимает
   ему право печати на очередь (`Set-Printer`/ACL) до начала следующего
   периода. Это отдельная и рискованная зона (легко случайно заблокировать
   всех) — делать отдельным пилотом.

5. **Алерты в Telegram/Email.**
   Простой периодический скрипт (тот же Task Scheduler) поверх
   `printaudit.queries`: сравнить накопленные `cost`/`pages` за период с
   порогом на пользователя/отдел из конфига и отправить сообщение через
   Telegram Bot API или SMTP при превышении.

6. **Активация AD-групп для правил "по расписанию".**
   Сейчас применение правил AD-группа→отдел (`/admin/department-rules`)
   требует ручного клика "Применить" после dry-run — осознанно, чтобы
   изменение отделов было видимым действием, а не тихим фоновым процессом.
   Если это станет неудобно на 4 объектах, можно добавить туда же Task
   Scheduler задачу, вызывающую `apply_ad_department_sync()` по расписанию —
   код для этого уже есть (`printaudit/department_resolver.py`), не хватает
   только обёртки-CLI и записи каждого автоприменения в `audit_log` с явной
   меткой "автоматически", чтобы не путать с ручными изменениями.

7. **Пагинация длинных списков в админке.**
   `/admin/ad-users` и `/admin/administrators` сейчас показывают полный
   список без пагинации — приемлемо для пилота (сотни, не тысячи записей),
   но при росте числа импортированных пользователей AD стоит добавить
   постраничную навигацию (`printaudit.queries`-подобный `limit`/`offset`
   уже используется для отчётов, для админки такого пока нет).

8. **Синхронизация AD по расписанию, а не только по кнопке.**
   `/admin/ad-users/resync` и обнаружение принтеров сейчас запускаются
   вручную из UI. Для 4 объектов может быть удобнее завести отдельную
   задачу Task Scheduler (по аналогии с `deploy/register_collector_task.ps1`)
   на пару раз в день, чтобы новые сотрудники/группы AD подтягивались
   без участия администратора.

9. **Дальше по мониторингу/прогнозированию** (см.
   [docs/PRINTER_MONITORING_FORECASTING.md](PRINTER_MONITORING_FORECASTING.md)
   за тем, что уже сделано как MVP): подписанный автообновляемый пакет
   endpoint-агента (сейчас — ручная/GPO установка версии, которую собрал
   администратор, без механизма самообновления); реальная сборка и проверка
   `deploy/endpoint_agent.wxs` через WiX Toolset на тестовой машине (не
   выполнялась в среде разработки — нет WiX/сертификата); более точные
   прогнозные модели, если MVP-baseline'ов (`seasonal_naive`/
   `moving_average`/`exponential_smoothing`) со временем перестанет
   хватать точности — только ПОСЛЕ того, как накопится история для честного
   сравнения с текущими baseline по WAPE, не раньше; алертинг поверх
   прогноза риска простоя/аномальной нагрузки (сейчас это данные на
   карточке устройства, без уведомлений — см. пункт 5 выше про
   Telegram/Email-алерты, тот же механизм применим и здесь).
