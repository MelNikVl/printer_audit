fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
sqlalchemy==2.0.35
pydantic==2.9.2
PyYAML==6.0.2
python-multipart==0.0.9
alembic==1.13.3
ldap3==2.9.1
python-dotenv==1.0.1
argon2-cffi==23.1.0
httpx==0.27.2

# Опционально, только если БД = PostgreSQL (см. config/config.yaml -> db.url)
# psycopg2-binary==2.9.9

# Опционально, только если на площадке используется monitoring_source=direct_snmp
# (см. docs/PRINTER_MONITORING_FORECASTING.md, printaudit/monitoring/snmp_adapter.py) —
# для zabbix_api эта зависимость не нужна вообще, там только httpx (уже выше).
# pysnmp==6.2.6
